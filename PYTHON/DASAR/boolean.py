#BOOLEAN
# BOOLEAN digunakan untuk menentukan sebuah itu True atau False
# boolean itu bisa diaplikasi langsung menggunakan printer atau bisa menggunakan if else dan function
# sebuah boolean bernilai True pada sebuah string apabila string itu terisi bila string itu kosong ("") maka akan bernilai false
# sebuah boolean bernilai True pada sebuah number semua number kecuali 0 itu akan bernilai false
# sebuah boolean bernilai True pada sebuah list,tuple,dict,set apabila terisi didalamnya, akan bernilai False bisa tidak terisi sama sekali
# atau kosong.
# BOOLEAN JUGA DAPAT DI TERAPAKAN PADA FUNCTION DENGAN MENGGUNAKAN RETURN
# Contoh penulisan didalam pemogramannya
def tersangka():
    return {"AKU MEMANG CINTA"} # => jika kita isi dengan 0 atau kosong jika pada list,tuple,dict,set itu akan bernilai False
if tersangka():
    print("jUJUR")
else:
    print("Berbohong")

# PYTHON JUGA ERAT DENGAN YANG NAMANYA OPERATOR
# JADI PADA PYTHON ITU DIBAGI MENJADI BEBERAPA OPERATOR
# YANG PERTAMA ITU OPERATOR ARITMATIKA
# + (addition) // x + y (pertambahan)
# - (subctraction) // x - y (pengurangan)
# * (multiply) // x * y (perkalian)
# / (divison) // x / y (pembagian)
# % (modulus) // x % y (modulus)
# ** (exponentiation) // x ** y (perpangkatan)
# // (floordivison) // x // y (pembagian bulat) jadi untuk ini hasilnya akan bulat karena nanti sisa pembagian dari sini masuknya ke mod
